<%if @user.errors.any? %>
    <div id="error_explanation" class="round">
	<h2><%= pluralize(@user.errors.count, "error") %>
	prohibited this user from being saved</h2>
	<p>Please fix problems with the following fields:</p>
	<ul>
	<% @user.errors.full_messages.each do |err| %>
	    <li><%= err %></li>
	<% end %>
	</ul>
    </div>
<% end %>

	    
